// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for CertificatesFunction
 */
export interface CertificatesFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/certs/certificates.
 */
export class CertificatesFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: CertificatesFunctionProps) {
    super(scope, id, {
      description: 'src/certs/certificates.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs20.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/certs/certificates.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}